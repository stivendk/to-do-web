import { EnumPriorityPipe } from './enum-priority.pipe';

describe('EnumPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
